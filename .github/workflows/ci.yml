name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install Dependencies
        run: npm ci

      - name: Check Formatting
        run: npm run prettier:check

      - name: Lint
        run: npm run lint

      - name: Lint Styles
        run: npm run style-lint

      # The library build is also the type and template check — there is no standalone typecheck script.
      - name: Build Library
        run: npm run build:lib

      - name: Test Library
        run: npm test -- ngx-formidable --watch=false --browsers=ChromeHeadless

      - name: Test Demo
        run: npm test -- ngx-formidable-demo --watch=false --browsers=ChromeHeadless

      - name: Build Demo
        run: npm run build
